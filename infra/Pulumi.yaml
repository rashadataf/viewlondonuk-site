name: viewlondonuk
description: ViewLondon UK web infrastructure (Traefik reverse proxy + Next.js site) managed with Pulumi TypeScript and Docker.
runtime:
  name: nodejs
  options:
    typescript: true

config:
  pulumi:autonaming:
    value:
      pattern: "${name}-${project}-${stack}"

